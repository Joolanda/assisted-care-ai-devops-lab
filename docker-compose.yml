services:
  api:
    build: .
    container_name: assisted-care-ai-devops-api
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=local
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
